# 國中英文每日任務 — 系統規則

本 repo 是「國中英文每日任務 + 零用金」**互動網頁**系統。
- 互動網頁：<https://laichinan-design.github.io/grade8-english-daily/>（由 `index.html` + `daily/latest.json` 驅動）。
- 排程（claude.ai Routine）每平日 20:00 生成當日測驗，寫成 JSON 供網頁載入。
- **零用金（含 18%/年月複利、可自訂金額領取）由網頁瀏覽器(localStorage)帳本處理；排程只負責出題，不需管帳、不需計息。**

## 一、每日出題（排程執行）
- 對象：國中**二~三年級（grade 8–9，CEFR A2–B1）**。
- 取材：融合《**大家說英語**》日常生活會話風格 + **國二~國三標準課綱**字彙/文法（校園、家庭、旅遊、飲食、購物、興趣…）。
- 一篇 **≤300 字**短文，含 **10–15 個目標單字（每字附英文例句）**、**2–3 個文法概念**（主題每天可換）。
- 出 **15 題，分三區各 5 題**：閱讀理解 5、文法 5、單字 5。每題附中文解析。
- 產出兩檔並 `git commit & push`：
  - `daily/<date>.md`：人類可讀版（短文＋單字表(含例句)＋文法重點＋三區15題＋答案 key）。
  - `daily/<date>.json` 並**覆蓋** `daily/latest.json`（schema 見第二節），供網頁載入。

## 二、JSON schema（`daily/latest.json`，必須合法 JSON）
```json
{
  "date":"YYYY-MM-DD","title":"短文標題","level":"國中二~三年級 (Grade 8-9)","note":"",
  "passage":"...(段落用 \\n)",
  "vocab":[{"w":"單字","pos":"詞性","d":"中文","example":"該單字的英文例句"}],
  "grammar":["文法重點說明"],
  "sections":{
    "reading":[{"q":"題目","opts":["A","B","C","D"],"a":0,"ex":"中文解析"}],
    "grammar":[ ...5題 ],
    "vocab":[ ...5題 ]
  }
}
```
- 每個 `vocab` 都要有 `example` 英文例句；`sections` 的 reading/grammar/vocab **各恰好 5 題**；`a` 為 0-based 正解索引。

## 三、零用金（由網頁處理，排程不需碰）
- 完成當日測驗且**答對 ≥ 13/15** → 得 **100 元**（每日一次）。每月約 20 平日 ≈ **2000 元/月**。
- **領取**：可**自訂金額**（不必全領），每月一次。
- **保留餘額**：以 **18%/年、每月複利**滾存。
