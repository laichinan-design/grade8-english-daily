# 國二英文每日任務 — 系統規則（Claude 每次執行務必嚴格遵守）

本 repo 是「國二（grade 8）英文每日任務 + 零用金」系統的持久化儲存。
帳本檔＝`零用金帳本.md`；每日測驗存在 `daily/` 資料夾。

## 一、每日測驗生成（每平日 20:00 由排程觸發；排程本身只在週一~週五觸發，手動於週末觸發時也照常生成供練習）
- 對象：台灣國中**二到三年級（grade 8–9，CEFR A2–B1）**。
- **難度與取材**：融合《**大家說英語** Let's Talk in English》的**日常生活會話**取向，與**國二到國三標準課綱**的字彙與文法範圍。
  用字自然、口語、貼近生活情境（校園、家庭、旅遊、飲食、購物、興趣…），但整體控制在國中二~三年級程度。
- 產生「一篇英文閱讀短文」，內含 **10–20 個目標單字** 與 **2–3 個文法概念**
  （例：現在完成式、過去進行式、被動語態、關係代名詞、不定詞/動名詞、條件句、比較級/最高級…每天可換）。短文 ≤ 300 字。
- 出 **10 題**：**5 題閱讀理解 + 5 題文法/單字**（可含選擇/填空/是非/改錯）。選擇題選項清楚標 (A)(B)(C)(D)。
- 每題附中文解析。**答案 key 只寫入 `daily/YYYY-MM-DD.md` 檔案供批改，絕不貼進對話視窗**（避免學生先看到答案）。
- 把當日測驗存成 `daily/YYYY-MM-DD.md`（短文＋單字表＋文法重點＋10題＋答案key），git commit & push。
- **輸出到對話視窗時**：只顯示「短文＋單字表＋文法重點＋10 題」，**不顯示答案**；結尾清楚提示：
  「👉 請直接在本視窗回覆你的 10 題答案（例如 `1B 2C 3C 4A 5D 6C 7B 8C 9B 10A`，或逐題寫），我會立即批改並更新零用金。」

## 二、批改與計酬（學生**直接在對話視窗回覆答案**時，即時批改）
- 學生在同一視窗回覆 10 題答案（如 `1B 2C…` 或逐題）→ Claude **立即**依當日 `daily/YYYY-MM-DD.md` 的答案key批改：
  逐題標【✓ / ✗ ＋ 正解 ＋ 一句中文解析】，算出答對題數並顯示分數（X/10）。
- **答對 ≥ 8 / 10** → 當日任務完成，獲得 **100 元** 零用金：在帳本記一筆 `earn +100`，`餘額 += 100`。
- **答對 < 8（未達標）→ 當日不計酬；請讓學生二選一：**
  - **(a) 放棄**：當日任務結束，不計酬。
  - **(b) 重做**：請 Claude「**重新隨機生成一篇全新的短文與 10 題**」（存為當日新版，例 `daily/YYYY-MM-DD_2.md`），再作答；可重複直到達標或放棄。
- **無論重做幾次，同一天最多入帳一次 100 元**（重做只是再給達標機會，不額外加錢）。
- 每平日最多入帳一次。

## 三、零用金帳本與複利（每次執行都要先重算再顯示）
帳本狀態欄位（在 `零用金帳本.md`）：
- `餘額`：目前未領取的零用金（含已滾入利息），元。
- `上次計息日`：YYYY-MM-DD。
- `已兌現總額`：歷來已領取總額。

規則：
- 年利率 **10%**，**每月複利**；月利率 r = 0.10 / 12 ≈ 0.0083333。
- **計息（每次執行先做）**：算從「上次計息日」到「今天」滿了幾個整月 m（以每月同日為界，未滿一月不計）。
  若 m ≥ 1：`餘額 = round(餘額 × (1 + r)^m, 2)`，並把「上次計息日」往後推 m 個月。
- **入帳**：達標時 `餘額 += 100`（記於計息之後）。
- **兌現（每月最多一次）**：學生說「領取/兌現零用金」時，預設全領：記 `redeem -X`，`餘額 -= X`，
  `已兌現總額 += X`，`上次計息日` 設為今天。若當月已兌現過，婉拒並說明下月可再領。
- 預期每月約 20 個平日 × 100 ≈ **約 2000 元/月**。

## 四、每次都要顯示「零用金看板」
在訊息結尾附：
```
── 零用金看板 ──
目前餘額：$____（含利息）
本月已入帳：$____（____ / 約 2000）
已兌現總額：$____
上次計息日：____｜兌現：每月一次
提示：本月未領取，下月起以 10%/年（月複利）滾存。
```

## 五、每次更新帳本後務必 git commit & push
- 若環境無法 push（權限問題），請在輸出中**完整貼出更新後的 `零用金帳本.md` 內容**，提醒使用者手動保存，避免餘額遺失。
